# Continuous integration: compila y ejecuta los tests en los TRES sistemas con
# cada push a main y cada pull request. Es la red de seguridad rápida — el
# release.yml luego se encarga de empaquetar para Linux/Windows/macOS al hacer
# un tag.
#
# La matriz existe porque hasta 2026-07-25 los tests solo corrían en Linux: los
# binarios de Windows y macOS se publicaban sin que se hubiera ejecutado ni una
# prueba sobre ellos (release.yml compila `--target md-editor`, ni siquiera
# construye los tests). Todo lo que depende del sistema —rutas, separadores,
# mayúsculas del sistema de ficheros, fuentes— era terreno ciego.
name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  # A mano, para el barrido de versiones de Qt (job `qt-version-sweep`), que no
  # tiene sentido pagar en cada push: solo cambia cuando sale una versión de Qt.
  workflow_dispatch:

jobs:
  build-test:
    name: Build & Test (${{ matrix.name }})
    runs-on: ${{ matrix.os }}
    strategy:
      # Que un sistema falle no cancela a los otros dos: interesa ver el cuadro
      # completo en cada push, no el primer fallo.
      fail-fast: false
      matrix:
        include:
          - name: Linux
            os: ubuntu-latest
          - name: Windows
            os: windows-latest
            qt_arch: win64_msvc2022_64
          # macos-14, no macos-latest: la imagen reciente rodó a un SDK que
          # eliminó el framework AGL que los binarios de Qt 6.8.2 todavía
          # referencian (`ld: framework 'AGL' not found`). Misma imagen que usa
          # release.yml para el DMG.
          - name: macOS
            os: macos-14
    steps:
      - uses: actions/checkout@v5

      # Qt 6.8.2 (la misma versión con la que está desarrollado el proyecto).
      # `cache: true` reusa la instalación entre runs del mismo runner image.
      - name: Install Qt
        uses: jurplel/install-qt-action@v4
        with:
          version: '6.8.2'
          cache: true
          arch: ${{ matrix.qt_arch }}

      # Dependencias del sistema que Qt necesita en runtime (OpenGL para los
      # widgets compuestos, libxkbcommon para teclado, fontconfig para
      # texto). El runner ya trae compilador y CMake. Windows y macOS no
      # necesitan nada de esto.
      - name: Install system dependencies (Linux)
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libgl1-mesa-dev libxkbcommon-x11-0 libfontconfig1 libdbus-1-3 \
            libegl1 libxcb-cursor0 libxcb-icccm4 libxcb-image0 libxcb-keysyms1 \
            libxcb-randr0 libxcb-render-util0 libxcb-shape0 libxcb-sync1 \
            libxcb-xfixes0 libxcb-xinerama0 libxcb-xkb1 libxkbcommon0

      # Hunspell para compilar el corrector de verdad (no el stub) y que sus
      # pruebas corran (con un diccionario instalado). Solo en Linux, que es
      # donde release.yml también lo tiene: en Windows y macOS los binarios
      # publicados salen HOY sin corrector, así que la matriz prueba lo que de
      # verdad se distribuye. Si algún día la release los compila con Hunspell,
      # este paso tiene que crecer con ella (`brew install hunspell`, vcpkg).
      # Sin él, SpellChecker es un stub inerte y tst_spellchecker se salta solo.
      - name: Install Hunspell (Linux)
        if: runner.os == 'Linux'
        run: sudo apt-get install -y libhunspell-dev hunspell-en-us

      - name: Configure
        run: cmake -S . -B build -DCMAKE_BUILD_TYPE=Release

      # `--parallel 2` (no todos los núcleos): compilar/enlazar las ~50 unidades de
      # test a la vez agota la RAM del runner (cc1plus «Terminated», exit 143). Mismo
      # tope que el job de sanitizers; la release lo evita compilando solo el binario.
      # `--config Release` lo necesita el generador multiconfiguración de Windows
      # (MSVC); en Linux y macOS se ignora.
      - name: Build
        run: cmake --build build --config Release --parallel 2

      # Las pruebas usan QApplication: corren sin pantalla (CMake ya fija la
      # plataforma en cada test, pero lo replicamos por si acaso). En Windows,
      # el plugin nativo en vez de «offscreen»: ver el porqué en CMakeLists,
      # donde está el mismo condicional (offscreen ahí rompe la serialización
      # del énfasis y silencia la salida de QTest).
      # `-C Release`, igual que arriba, es para el generador de Windows.
      - name: Test
        run: ctest --test-dir build --output-on-failure -C Release
        env:
          QT_QPA_PLATFORM: ${{ runner.os == 'Windows' && 'windows' || 'offscreen' }}

      # Un test que se cae ANTES de arrancar (falta una DLL, peta en la
      # inicialización estática) no imprime ni la cabecera de QTest, y
      # --output-on-failure se queda en blanco: no hay nada que enseñar. Con -VV
      # sí aparecen al menos la orden exacta y el directorio de trabajo.
      - name: Re-run failed tests verbosely
        if: failure()
        run: ctest --test-dir build --rerun-failed --output-on-failure -VV -C Release
        env:
          QT_QPA_PLATFORM: ${{ runner.os == 'Windows' && 'windows' || 'offscreen' }}

      # En Windows, un test que falla no escribe nada en stdout —ni por ctest ni
      # ejecutándolo a mano—, así que el mensaje de la aserción no aparece por
      # ningún lado. El logger a fichero de QTest (`-o fichero,txt`) no depende
      # de la consola y sí lo da. El código de salida se imprime también: un
      # número pequeño es QTest contando funciones fallidas, y 0xC0000005 o
      # 0xC00000FD serían un cuelgue.
      - name: Failure detail of the test binaries (Windows)
        if: failure() && runner.os == 'Windows'
        shell: pwsh
        env:
          QT_QPA_PLATFORM: windows
        run: |
          foreach ($exe in Get-ChildItem build/Release/tst_*.exe) {
            $log = Join-Path $env:RUNNER_TEMP "$($exe.BaseName).txt"
            & $exe.FullName -o "$log,txt" 2>&1 | Out-Null
            if ($LASTEXITCODE -ne 0) {
              Write-Host "=== $($exe.Name): exit=$LASTEXITCODE"
              if (Test-Path $log) {
                Get-Content $log | Where-Object { $_ -match 'FAIL!|Actual|Expected|Loc:' } |
                  Select-Object -First 12 | ForEach-Object { Write-Host "    $_" }
              }
            }
          }
          exit 0

      # `QTextDocument::setMarkdown` mata el proceso en Windows (0xC0000005) con
      # un documento que empieza y acaba por regla temática `---` sin salto de
      # línea final. Es un fallo de Qt —`qtmdrepro` enlaza SOLO Qt, sin
      # md-editor-core— y el editor lo rodea añadiendo ese `\n` en
      # `mdrender::protect()`. Ver el comentario largo en tools/qtmdrepro.cpp.
      #
      # Este paso es el CENTINELA del rodeo, no una guardia: informa y no falla.
      # Lo que hay que mirar es cuándo `minimo` DEJA de caer, porque eso
      # significará que Qt lo ha arreglado y que se puede retirar el rodeo.
      # Una variante por proceso, que el fallo mata el proceso.
      - name: Qt setMarkdown crash sentinel (Windows)
        if: always() && runner.os == 'Windows'
        shell: pwsh
        env:
          QT_QPA_PLATFORM: windows
        run: |
          $exe = "build/Release/qtmdrepro.exe"
          # Sin argumentos lista las variantes: el número vive en el .cpp, no aquí.
          $listado = & $exe
          $total = [int]$listado[0]
          Write-Host "=== $total variantes, una por proceso"
          for ($i = 0; $i -lt $total; $i++) {
            $nombre = ($listado[$i + 1] -split ' ', 2)[1]
            & $exe $i 2>&1 | Out-Null
            $code = $LASTEXITCODE
            $marca = if ($code -eq 0) { "ok  " } else { "CAE " }
            Write-Host ("    {0}variante {1,2} ({2}): exit={3}" -f $marca, $i, $nombre, $code)
          }
          exit 0

      # Y la comprobación de que el rodeo hace su trabajo donde se descubrió el
      # fallo: el fuzzer completo, que hasta ahora moría en el caso 281. No está
      # registrado en ctest (ver CMakeLists), así que se ejecuta a mano. Informa
      # y no falla: la decisión de devolverlo o no al gate es aparte.
      - name: Round-trip fuzzer (Windows, informativo)
        if: always() && runner.os == 'Windows'
        shell: pwsh
        env:
          QT_QPA_PLATFORM: windows
        run: |
          & build/Release/tst_roundtripfuzz.exe 2>&1 | Out-Null
          Write-Host "=== tst_roundtripfuzz: exit=$LASTEXITCODE"
          exit 0

  sanitizers:
    name: Sanitizers (ASan/UBSan)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - name: Install Qt
        uses: jurplel/install-qt-action@v4
        with:
          version: '6.8.2'
          cache: true

      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libhunspell-dev hunspell-en-us \
            libgl1-mesa-dev libxkbcommon-x11-0 libfontconfig1 libdbus-1-3 \
            libegl1 libxcb-cursor0 libxcb-icccm4 libxcb-image0 libxcb-keysyms1 \
            libxcb-randr0 libxcb-render-util0 libxcb-shape0 libxcb-sync1 \
            libxcb-xfixes0 libxcb-xinerama0 libxcb-xkb1 libxkbcommon0

      # Build de diagnóstico: instrumenta todo con ASan + UBSan y aborta al primer
      # error de memoria / comportamiento indefinido. Debug para mejores trazas.
      - name: Configure
        run: cmake -S . -B build -DCMAKE_BUILD_TYPE=Debug -DENABLE_SANITIZERS=ON

      # `--parallel 2` (no todos los núcleos): compilar con ASan instrumenta cada
      # TU y dispara mucho la RAM por cc1plus; con todos los núcleos a la vez el
      # runner se queda sin memoria (cc1plus «Killed») en las TUs más pesadas.
      - name: Build
        run: cmake --build build --parallel 2

      # detect_leaks=0: el ruido de fugas a la salida es de Qt/fontconfig, no
      # nuestro; nos interesan los errores de memoria y el UB.
      - name: Test (bajo sanitizers)
        run: ctest --test-dir build --output-on-failure
        env:
          QT_QPA_PLATFORM: offscreen
          ASAN_OPTIONS: detect_leaks=0
          UBSAN_OPTIONS: print_stacktrace=1:halt_on_error=1

  # El fuzzer del round-trip, en su propio job y SIN capacidad de bloquear
  # (`continue-on-error`). Las dos mitades de esa decisión importan:
  #
  #   • Job aparte y no bloqueante porque mete entrada adversaria por el pipeline
  #     de carga/serialización, y un fallo suyo no debe impedir publicar una
  #     release: su papel es avisar, no vetar. `continue-on-error` (en vez de
  #     tragarse el código de salida con `exit 0`) deja el fallo VISIBLE en la
  #     interfaz de Actions; lo que no hace es tumbar el workflow.
  #
  #   • Bajo sanitizers porque hasta ahora NO se ejecutaba con ellos, al
  #     contrario de lo que decían sus comentarios: no está registrado en ctest
  #     (a propósito, ver CMakeLists) y el job de sanitizers corre `ctest`, así
  #     que la herramienta que cazaría un fallo de memoria en el fuzzeo no se le
  #     había aplicado nunca. Aquí se invoca el ejecutable directamente, que es
  #     la única forma de que ASan/UBSan lleguen a él.
  #
  # En Windows se ejecuta dentro de build-test, no aquí: allí el binario ya está
  # compilado y correrlo sale gratis, mientras que un job propio obligaría a
  # rehacer toda la build de Windows para 2000 casos.
  fuzz:
    name: Round-trip fuzzer (ASan/UBSan, informativo)
    runs-on: ubuntu-latest
    continue-on-error: true
    # Un cuelgue del fuzzer no debe quemar el presupuesto de Actions: sin tope,
    # un caso que no termine se lleva las 6 horas por defecto.
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v5

      - name: Install Qt
        uses: jurplel/install-qt-action@v4
        with:
          version: '6.8.2'
          cache: true

      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libhunspell-dev hunspell-en-us \
            libgl1-mesa-dev libxkbcommon-x11-0 libfontconfig1 libdbus-1-3 \
            libegl1 libxcb-cursor0 libxcb-icccm4 libxcb-image0 libxcb-keysyms1 \
            libxcb-randr0 libxcb-render-util0 libxcb-shape0 libxcb-sync1 \
            libxcb-xfixes0 libxcb-xinerama0 libxcb-xkb1 libxkbcommon0

      - name: Configure
        run: cmake -S . -B build -DCMAKE_BUILD_TYPE=Debug -DENABLE_SANITIZERS=ON

      # --parallel 2: igual que el job de sanitizers, compilar instrumentado
      # dispara la RAM de cc1plus y con todos los núcleos el runner se queda sin
      # memoria.
      - name: Build the fuzzer
        run: cmake --build build --parallel 2 --target tst_roundtripfuzz

      # Directo al ejecutable: no está en ctest, así que esta es la única vía.
      - name: Fuzz
        run: ./build/tst_roundtripfuzz
        env:
          QT_QPA_PLATFORM: offscreen
          ASAN_OPTIONS: detect_leaks=0
          UBSAN_OPTIONS: print_stacktrace=1:halt_on_error=1

  # ¿En qué versiones de Qt está el cuelgue de `setMarkdown` en Windows? Es la
  # primera pregunta de cualquier reporte aguas arriba («¿lo has probado en la
  # última?»), y también la que dirá cuándo se puede retirar el rodeo de
  # `mdrender::protect()`.
  #
  # Solo `workflow_dispatch`: la respuesta únicamente cambia cuando sale una
  # versión nueva de Qt, así que no se paga en cada push. Se lanza a mano con
  #     gh workflow run ci.yml
  # Compila SOLO `qtmdrepro` (que enlaza únicamente Qt6::Widgets), no el
  # proyecto entero, así que cada versión cuesta poco más que la descarga de Qt.
  qt-version-sweep:
    name: Qt sweep — setMarkdown crash (Windows, Qt ${{ matrix.qt }})
    if: github.event_name == 'workflow_dispatch'
    runs-on: windows-latest
    continue-on-error: true
    timeout-minutes: 30
    strategy:
      # Que falte una versión o falle una no debe ocultar el resto del cuadro:
      # lo que interesa es la tabla completa.
      fail-fast: false
      matrix:
        qt: ['6.8.2', '6.9.*', '6.10.*', '6.11.*']
    steps:
      - uses: actions/checkout@v5

      - name: Install Qt
        uses: jurplel/install-qt-action@v4
        with:
          version: ${{ matrix.qt }}
          arch: win64_msvc2022_64
          cache: true

      - name: Configure
        run: cmake -S . -B build -DCMAKE_BUILD_TYPE=Release

      - name: Build the repro
        run: cmake --build build --config Release --target qtmdrepro

      # Una variante por proceso: el fallo mata el proceso.
      - name: Run the variants
        shell: pwsh
        env:
          QT_QPA_PLATFORM: windows
        run: |
          $exe = "build/Release/qtmdrepro.exe"
          $listado = & $exe
          $total = [int]$listado[0]
          Write-Host "=== Qt ${{ matrix.qt }} (real: $(qmake -query QT_VERSION))"
          for ($i = 0; $i -lt $total; $i++) {
            $nombre = ($listado[$i + 1] -split ' ', 2)[1]
            & $exe $i 2>&1 | Out-Null
            $code = $LASTEXITCODE
            $marca = if ($code -eq 0) { "ok  " } else { "CAE " }
            Write-Host ("    {0}variante {1,2} ({2}): exit={3}" -f $marca, $i, $nombre, $code)
          }
          exit 0

      # La traza del reporte se capturó sobre el documento grande, no sobre el
      # caso mínimo que el reporte enseña. Aquí se saca donde toca, con el
      # depurador de consola del Windows SDK que traen los runners: `-g -G` se
      # salta los puntos de parada inicial y final, y `-c "g;kv;q"` arranca,
      # espera al fallo y vuelca la pila con argumentos.
      - name: Stack trace of the minimal case
        shell: pwsh
        env:
          QT_QPA_PLATFORM: windows
        run: |
          $cdb = "C:\Program Files (x86)\Windows Kits\10\Debuggers\x64\cdb.exe"
          if (-not (Test-Path $cdb)) {
            Write-Host "cdb.exe no está en la imagen; sin traza."
            exit 0
          }
          # Variante 0 = `minimo` (---\n\n---), la que debe caer.
          & $cdb -g -G -c "g;kv;q" build/Release/qtmdrepro.exe 0 2>&1 |
            Select-String -Pattern 'Qt|qtmdrepro|Access violation|ExceptionCode|^[0-9a-f]{8}' |
            Select-Object -First 40 | ForEach-Object { Write-Host $_ }
          exit 0

  clang-tidy:
    name: clang-tidy
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - name: Install Qt
        uses: jurplel/install-qt-action@v4
        with:
          version: '6.8.2'
          cache: true

      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            clang-tidy libhunspell-dev \
            libgl1-mesa-dev libxkbcommon-x11-0 libfontconfig1 libdbus-1-3 \
            libegl1 libxcb-cursor0 libxcb-icccm4 libxcb-image0 libxcb-keysyms1 \
            libxcb-randr0 libxcb-render-util0 libxcb-shape0 libxcb-sync1 \
            libxcb-xfixes0 libxcb-xinerama0 libxcb-xkb1 libxkbcommon0

      # Configurar genera compile_commands.json (CMAKE_EXPORT_COMPILE_COMMANDS) y
      # el código de moc; build asegura que todo lo generado existe.
      - name: Configure
        run: cmake -S . -B build -DCMAKE_BUILD_TYPE=Release

      # `--parallel 2`: igual que arriba, compilar todos los tests a la vez agota la
      # RAM del runner (exit 143). Este job necesita compilar para generar el moc.
      - name: Build
        run: cmake --build build --parallel 2

      # Análisis estático con los checks del .clang-tidy del repo; falla ante
      # cualquier aviso (el código está limpio, así que es una guardia de
      # regresión). Sobre todos los .cpp de src/ (recursivo: viven en
      # subdirectorios por componente); no las pruebas ni el código generado.
      - name: clang-tidy
        run: clang-tidy -p build $(find src -name '*.cpp')
